language: php

php:
  - 7.3

env:
  - SYMFONY_VERSION="4.4.*"

before_script:
  - curl -s http://getcomposer.org/installer |php --
  - php composer.phar require symfony/symfony:${SYMFONY_VERSION}

script: phpunit
